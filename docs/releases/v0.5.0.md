# Axiom v0.5.0 — P0-2 durable execution and recovery

Status: Completed

Priority track: P0-2

## Outcome

Agent runs survive process boundaries as inspectable SQLite checkpoints and can continue from known
model/tool state without silently replaying an operation whose outcome is uncertain.

## Scope

- [x] Persist runs, plans, steps, attempts, model turns, and tool calls with stable IDs.
- [x] Add `axiom runs`, `runs show`, `runs export`, and `axiom resume`.
- [x] Add `axiom demo recovery` as a one-command, offline P0-2 acceptance check.
- [x] Restore completed steps and known tool outputs when resuming.
- [x] Block automatic recovery when an interrupted tool may already have caused side effects.
- [x] Distinguish failed, cancelled, interrupted, skipped, and blocked lifecycle states.
- [x] Persist sanitized EventBus events in SQLite while retaining `.axiom/events.jsonl`.
- [x] Account separately for Planner, Executor, and Finalizer calls, tokens, failures, and latency.
- [x] Add a TUI Runs dialog with history, lifecycle and step details, stage metrics, resume, guarded
  uncertain retry, and sanitized event export.
- [x] Add `Ctrl+R` and `/runs`, `/show`, `/resume`, `/retry`, and `/export` convenience entry points.
- [x] Restore a completed run's saved transcript and context for follow-up work.
- [x] Make new-run reset and terminal-safe submit/newline controls consistent across the TUI.

## Acceptance

- [x] Unit tests pass on Ubuntu and Windows with Python 3.11 and 3.14.
- [x] A deterministic interruption test resumes after a completed tool without replaying it.
- [x] An uncertain tool outcome blocks recovery unless replay is explicitly authorized.
- [x] Users can exercise both recovery paths without a model key, MCP server, or timed interruption.
- [x] TUI tests cover history/detail rendering, export, normal resume, direct commands, and mandatory
  confirmation before uncertain-tool replay.
- [x] TUI tests cover real send/newline key events, completed-run context selection, and full
  transcript/activity reset when starting a new run.
- [x] Ruff, Mypy, bytecode compilation, core Eval, and wheel inspection pass.
- [x] The built wheel reports Axiom 0.5.0 and contains the packaged evaluation suite.

## Deferred

- Transactional file patches, rollback, and agent-owned change tracking remain P0-3.
- General retry deduplication and side-effect-aware retry budgets remain P0-6.
- Cross-process leases and concurrent writers are not yet supported; one active Axiom process should
  own a workspace run database at a time.

## Verification log

- 2026-09-12: local verification passed 35/35 tests, Ruff, Mypy across 31 source files,
  bytecode compilation, the 8/8 deterministic core Eval, CLI smoke tests, and wheel inspection.
- 2026-09-12: GitHub Actions run `34670168802` passed Ruff/Mypy and the complete test/Eval suite
  on Ubuntu and Windows with Python 3.11 and 3.14.
- 2026-09-14: the recovery acceptance demo passed 5/5 checks; the full suite passed 36/36 tests,
  8/8 core Eval cases, Ruff, and Mypy. GitHub Actions run `34804695555` passed all five jobs on
  Ubuntu and Windows with Python 3.11 and 3.14.
- 2026-09-14: TUI recovery parity passed 39/39 tests, including run history/detail, event export,
  normal resume, direct slash commands, and mandatory confirmation for uncertain-tool replay;
  8/8 core Eval cases, Ruff, Mypy, and bytecode compilation also passed locally.
- 2026-09-15: terminal-key, new-run reset, and historical-context continuation coverage brought the
  suite to 42/42 passing tests; 8/8 core Eval cases, Ruff, Mypy, and bytecode compilation passed.
